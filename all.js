function all(arr) {
	// Loop through every item in the array
	for (let i = 0; i < arr.length; i++){
	if(!arr[i]) return false
	// If the item evaluates to false, return false
	// If the loop ends, return true
}
	return true
}


console.log(all([2 === 2, 2 === 2]))
